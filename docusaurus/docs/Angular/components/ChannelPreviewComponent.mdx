The `ChannelPreview` component displays a channel preview in the channel list, it consists of the image, name and latest message of the channel.

## Customization

The channel list uses the `ChannelPreview` component to display channels, if you want to replace that with your own custom component, here is how to do it:

**Example 1** - custom channel preview component:

```html
<stream-channel-list
  [customChannelPreviewTemplate]="customChannelPreviewTemplate"
></stream-channel-list>
<stream-channel>
  <stream-channel-header></stream-channel-header>
  <stream-message-list></stream-message-list>
  <stream-message-input></stream-message-input>
</stream-channel>

<ng-template #customChannelPreviewTemplate let-channel="channel">
  <custom-channel-preview [channel]="channel"></custom-channel-preview>
</ng-template>
```

Your custom template can receive the same inputs (with the same name) as the default `ChannelPreview` component.

:::note

If you want to build your own `ChannelPreview` component, you might find the following building blocks useful:

### Avatar

The `ChannelPreview` component uses the [`Avatar`](./AvatarComponent.mdx) component to display the channels's photo.

:::

[//]: # "Start of generated content"
## Inputs and outputs 


### channel

• **channel**: `undefined` \| `Channel`<`UR`, `UR`, `LiteralStringForUnion`, `UR`, `UR`, `UR`, `UR`\>

The channel to be displayed

#### Defined in

[lib/channel-preview/channel-preview.component.ts:23](https://github.com/GetStream/stream-chat-angular/blob/f5f7740/projects/stream-chat-angular/src/lib/channel-preview/channel-preview.component.ts#L23)

[//]: # "End of generated content"
